---
import "../../styles/global.scss";
import { firestore } from "../../firebase/server";

// const API_URL: string = import.meta.env.APP_BASE_URL;

// if (Astro.request.method === "POST") {
//   const response = await fetch(`${API_URL}/api/guests/guests`, {
//     method: "POST",
//     body: JSON.stringify({hallo: "hallo"}),
//     headers: {
//       "Content-Type": "application/json"
//     },
//   });

//   if (response.ok) {
//     const data = await response.json();
//     console.log(data);
//   } else {
//     console.error("Error");
//   }
// }


const collectionRef = firestore.collection("guests");
const collectionSnapshot = await collectionRef.get();
const guests = collectionSnapshot.docs.map((doc) => doc.data());
---

<div class="guest-container">
  <div class="guest">
    {
    guests.map((guest) => (
    <li>
      <p>{guest.firstName + " " + guest.lastName}</p>
      <p>{guest.reference}</p>
    </li>
    ))
    }
  </div>
</div>

<!-- <form method="POST"> -->
<!--   <button class="button" type="submit">Upload</button> -->
<!-- </form> -->
